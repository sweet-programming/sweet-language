fib = @(x: Int, memo: Array) {
  return 0 if x == 0
  return 1 if x == 1

  memo[x - 1] = fib(x - 1, memo) + fib(x - 2, memo) unless memo[x - 1]?
  memo[x - 1]
}

main = @{
  print("fib(40) = " + fib(40, Array(40)) + "\n")
}
