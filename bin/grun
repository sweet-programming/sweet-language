#!/bin/sh

case "$0" in
/*) bindir=`dirname "$0"` ;;
*) bindir=`dirname "$PWD/$0"` ;;
esac

bindir=`cd -P $bindir;pwd`
libdir=`cd -P $bindir/../lib;pwd`

CLASSPATH=.:$libdir/antlr-4.2-complete.jar

java -classpath ${CLASSPATH} org.antlr.v4.runtime.misc.TestRig $@
